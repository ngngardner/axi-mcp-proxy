{
  "mcpServers": {
    "axi": {
      "type": "stdio",
      "command": "cargo",
      "args": ["run", "--", "--config", "dogfood.ncl", "--transport", "stdio"],
      "env": {
        "CARGO_TARGET_DIR": "target/mcp"
      }
    }
  }
}
