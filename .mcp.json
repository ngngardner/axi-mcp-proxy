{
  "mcpServers": {
    "axi": {
      "type": "stdio",
      "command": "cargo",
      "args": ["run", "--", "--config", "examples/dogfood/config.ncl", "--transport", "stdio"],
      "env": {
        "CARGO_TARGET_DIR": "target/mcp"
      }
    }
  }
}
